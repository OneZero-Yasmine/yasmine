# Yasmine 个人网站项目说明与开发规范

状态：现役维护文档
最后核对：2026-08-18

本文档是本项目的开发、内容、测试和发布规范。开始任何修改前，先阅读本文档；如果文档与当前代码或自动化测试不一致，以已验证的代码与运行结果为事实依据，并在同一个 PR 中同步修正文档。

## 1. 项目定位与当前状态

本项目是 Yasmine 的静态个人网站。视觉语言为“古典书页 × 当代作品集”，内容覆盖个人介绍、产品与创业项目、实习、研究、随笔和媒体报道。

- 线上地址：<https://onezero-yasmine.github.io/yasmine/>
- GitHub 仓库：<https://github.com/OneZero-Yasmine/yasmine>
- 默认分支：`main`
- 技术形态：纯静态网站，无后端、数据库、账号系统、CMS 或用户数据存储
- 页面生成：Eleventy 3 + Nunjucks
- 样式与交互：原生 CSS、原生 JavaScript
- 部署：GitHub Actions 构建，通过后发布到 GitHub Pages
- Node.js：本地最低 22，CI 使用 24

现役页面共 10 个：

1. 首页 `/`
2. About `/about/`
3. Work `/work/`
4. Notes `/notes/`
5. Awards `/awards/`
6. CalcAI `/project/calculus-ai/`
7. Tiger Begins `/project/tiger/`
8. Time Widget `/project/time-widget/`
9. Voxsee `/project/voxsee/`
10. 自定义 404 `/404.html`

## 2. 权威信息与文档分工

同一事实只维护一个权威来源：

| 内容 | 权威来源 |
|---|---|
| 个人经历、作品事实、公开范围 | 用户明确确认的信息与本地 `10的真实详细资料/` |
| 生产页面文案与条目 | `src/_data/*.json` |
| 页面结构与组件 | `src/*.njk`、`src/_includes/` |
| 视觉与交互实现 | `src/assets/css/site.css`、`src/assets/js/site.js` |
| 路由、子路径与构建规则 | `eleventy.config.js` |
| 自动检查的行为合同 | `tests/`、`scripts/validate-content.mjs` |
| CI 与部署流程 | `.github/workflows/ci.yml` |
| 开发与维护规范 | 本文件 `项目.md` |
| 快速安装与运行入口 | `README.md` |
| AI 必须遵守的项目级规则 | `AGENTS.md` |
| 公开身份与隐私边界 | 仅使用英文名 `Yasmine`，其他信息以用户明确确认的公开范围为准 |

`PRD-个人网站.md` 和 `”10 的个人网站“完整开发计划.md` 是开发前的历史规划，仅用于追溯设计意图；其中的页面内容、编号、状态和发布说法不代表当前实现。发现历史文档与现役代码冲突时，不得为了匹配历史文档而回退现役网站。

## 3. 目录说明

```text
src/
  _data/                 生产内容与导航数据
  _includes/             布局和复用组件
  assets/                已获准公开的网页资源、样式和脚本
  *.njk                  页面模板
scripts/
  build-assets.mjs       从本地母版生成网页衍生资源
  validate-content.mjs   内容、资源、链接和视频预算校验
tests/
  site.test.mjs          页面、内容、链接与隐私合同测试
  browser-smoke.mjs      桌面端和移动端浏览器冒烟测试
.github/workflows/
  ci.yml                 PR 检查与 main 自动部署
_site/                   构建产物，不提交
tmp/                     本地测试产物，不提交
```

以下目录包含原始资料或开发参考，已被 `.gitignore` 排除，不得提交到公开仓库：

- `10的真实详细资料/`
- `每一页的UI/`
- `素材/`
- `contact-icons/`
- `docs/`

原始简历、个人资料、照片母版、二维码和未确认联系方式不得直接复制到生产目录。公开站点只使用经过用户确认、位于 `src/assets/` 的衍生文件。

## 4. 本地运行与验证

首次安装：

```bash
npm install
```

生成或更新网页素材：

```bash
npm run assets
```

启动本地预览：

```bash
npm run dev
```

正式构建与完整测试：

```bash
npm run build
npm test
```

`npm run build` 会先清理 `_site/`，校验内容，然后生成生产网站。`npm test` 依赖已经生成的 `_site/`，因此完整验收必须按上述顺序运行。

不得通过删除测试、放宽断言、注释校验或提交 `_site/` 来“修复”失败。先确认需求是否改变；如果行为确实需要改变，应同时修改实现、对应测试和本文档。

## 5. 强制开发流程

任何功能、文案、图片、样式、依赖、测试或配置修改都必须遵循：

1. 从最新 `main` 新建独立分支，建议命名为 `codex/<简短任务名>`。
2. 修改前说明目标、范围和验收标准；不要顺手重构无关代码。
3. 在分支中完成最小必要修改。
4. 本地运行 `npm run build` 和 `npm test`。
5. 人工预览受影响页面；视觉改动至少检查桌面端和手机端。
6. 提交清晰的 commit，并创建 PR 指向 `main`。
7. PR 描述必须写明：修改目的、主要改动、验证结果、视觉或内容风险。
8. 等待必需检查 `build-and-test` 通过，并解决全部 PR 对话。
9. 仅在 PR 内容正确、分支基于最新 `main` 时合并。
10. 合并后等待 GitHub Pages 部署完成，再打开线上地址验收。

禁止直接向 `main` 推送、强制推送或删除 `main`。不要绕过分支保护或跳过失败的 CI。

一个 PR 只处理一类目标。文案更新、视觉重做、依赖升级和结构重构应尽量拆开，确保每个 PR 都容易理解、验证和回滚。

## 6. 内容修改规范

- 只能写入真实资料或用户明确确认的内容；不得根据视觉稿、上下文或常识补写经历、成绩、评价、日期、链接和媒体。
- 所有公开页面、页面元数据、结构化数据、仓库介绍、README、版权文字和项目文档统一使用英文名 `Yasmine`，不得出现真名、姓氏或其他可关联写法。
- 不得公开网站的内部用途、目标对象、申请意图、升学或就业安排等未公开个人规划；本地资料中存在这些信息也不代表获得公开授权。
- 每次修改公开文字后，都要检查源码、构建产物和仓库介绍，确保没有重新引入上述身份或规划信息。
- 优先修改 `src/_data/*.json`，不要把同一条内容复制进多个模板。
- 没有真实内容的栏目直接省略，不生成占位文案、假按钮、示例链接或 `CONTENT_TODO`。
- 新增或变更外链时，确认目标真实、可公开且使用 HTTPS。
- 新增图片必须提供准确 `alt`；装饰图使用空 `alt` 或 `aria-hidden`。
- 图片和视频应先生成适合网页的衍生文件，保留合理尺寸与体积；不得覆盖本地原始母版。
- 修改联系信息、简历信息、二维码或包含第三方人物的资料前，必须再次确认公开范围。
- 个人联系方式优先使用明确获准公开、可独立维护的邮箱或公众账号，不把手机号等私人标识嵌入公开联系方式。
- 隐私测试应检查敏感信息的类别、标签或格式，不得为了断言“页面不存在”而把待保护的精确值写进公开测试。
- 任何内容改动都要检查是否需要同步页面描述、canonical、sitemap、OG 图和相关测试。

## 7. 页面与设计修改规范

- 延续“古典书页 × 当代作品集”的现有视觉体系，优先复用 CSS token、公共布局和组件。
- 不为单次需求创建无必要的抽象、配置层或依赖。
- 不复制整页组件；通用结构放在 `src/_includes/`，项目详情继续使用数据驱动模板。
- 所有路由和资源必须兼容 GitHub Pages 的 `/yasmine/` 子路径，通过 Eleventy 的 `url` 过滤器或统一配置生成，不散落手写前缀。
- 保持语义 HTML、唯一 H1、键盘可操作、可见焦点、触屏可理解和 `prefers-reduced-motion` 支持。
- 不依赖 hover 才能获得关键信息；不自动播放视频；不加入持续背景动画或阻塞阅读的动效。
- 修改全站样式时必须同时检查首页、一个内容页、一个项目页和 404；修改公共组件时检查所有使用方。
- 不因“顺便优化”改写无关文案、格式化整个文件或重构稳定组件。

## 8. 测试与验收要求

所有 PR 的最低门槛：

- `npm run build` 成功。
- `npm test` 全部通过。
- 受影响页面无控制台错误、资源加载失败或横向溢出。
- 站内链接和 `/yasmine/` 资源路径正确。
- 私有资料目录没有进入 `_site/` 或 Git 追踪范围。

按改动类型追加验收：

| 改动类型 | 必须追加的检查 |
|---|---|
| 文案或数据 | 页面显示、事实准确性、隐私范围、相关合同测试 |
| 图片或视频 | 加载成功、比例、响应式表现、体积、替代文本 |
| CSS 或布局 | 1440×900 与 375×667，必要时检查中间断点 |
| JavaScript 交互 | 键盘、触屏、无 JS 回退、控制台错误 |
| 导航或路由 | 全部入口、返回路径、404、canonical、sitemap |
| 公共组件 | 所有使用该组件的页面 |
| 依赖或构建配置 | 干净安装、完整构建、完整测试、CI 一致性 |

出现线上问题时，优先回滚导致问题的 PR，恢复稳定版本后再从新分支修复；不要直接在 `main` 上热改。

## 9. CI、部署与分支保护

`.github/workflows/ci.yml` 在两类事件下运行：

- PR：安装依赖、构建、测试，不部署。
- 推送到 `main`：再次安装、构建和测试；全部通过后发布 `_site/` 到 GitHub Pages。

`main` 的保护规则应保持：

- 必须通过 PR 合并。
- 不要求他人审批，适配单人维护；如未来加入协作者再评估审批人数。
- 必需状态检查为 `build-and-test`，且合并前分支必须基于最新 `main`。
- 必须解决全部 PR 对话。
- 管理员不得绕过上述规则。
- 不允许强制推送，不允许删除分支。

修改工作流文件、必需检查名称或默认分支时，必须在同一个任务中同步更新分支保护与本文档，否则可能导致 PR 永远无法合并或未经测试即可合并。

## 10. 文档维护规范

- 改变安装、运行命令或公开使用方式：更新 `README.md`。
- 改变架构、目录、内容边界、开发流程、测试或部署：更新 `项目.md`。
- 改变 AI 必须每次执行的硬约束：精简更新 `AGENTS.md`，详细说明仍放在 `项目.md`。
- 历史决策和单次修复过程留在 Git commit 与 PR；不要把流水账堆进现役文档。
- 不创建内容重复的 `PLAN-v2.md`、`项目-新版.md` 或备份副本。需要修改时直接更新现役文档。
- 每次重要改版结束后，核对 README、`项目.md`、`AGENTS.md`、代码、测试和 CI 是否一致。

## 11. 维护节奏

- 每次修改：分支 → 本地验证 → PR → CI → 合并 → 部署 → 线上验收。
- 每月：抽查外链、联系方式、项目状态和主要页面资源。
- 每季度：评估依赖升级、移动端表现、加载体积和可访问性。
- 新增功能：先明确用户价值和验收标准，再实现；没有当前需求时不提前加入后端、CMS、分析统计、多语言或其他复杂系统。

项目的维护目标不是持续增加技术，而是让内容真实、页面稳定、修改可审查、问题可回滚。
